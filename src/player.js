class Player {
  constructor(id, wins) {
    this.id = id;
    this.wins = 0;
    this.hand = [];
  }

  playCard() {

    // move handArray.length-1 from hand to centerPile
  }

  saveWins(){
    // increment and add win to local storage
  }
}
