class Player {
  constructor() {
    this.id = Date.now();
    this.wins = 0;
    this.hand = [];
  }

  playCard() {
    // move index[0] from hand to centerPile
  }

  saveWins(){
    // add win to local storage
  }
}
